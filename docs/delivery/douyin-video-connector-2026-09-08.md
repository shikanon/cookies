# 抖音视频目录与选择器

## 行为

Connector 保存 `douyin_video` 对象。新迁移 `20260908110000_add_douyin_video_object_kind.up.sql` 扩展对象类型约束。

目录分页收集当前账户可用视频及作者信息，保留审核和可用性状态。编辑器支持视频 ID 或链接、名称及作者筛选，并通过鉴权预览展示封面。

切换作者或搜索条件时重新读取列表；无结果与输入格式错误分别反馈。Runner 使用平台原生视频选择器，按精确视频 ID 回读选中项，避免缓存列表和隐藏输入框导致误选。

## 验证

目录同步、分页和 HTTP 查询测试通过。真实页面已验证按视频和作者筛选、空结果、非法 ID 提示及封面预览。选择器操作只改变未保存草稿，不代表授权最终提交。
